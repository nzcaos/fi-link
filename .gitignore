# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/
ENV/

# Testing & linting caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/

# Django
*.log
db.sqlite3
db.sqlite3-journal
local_settings.py
/staticfiles/
/media/

# Secrets / local env
.env
.env.local

# Matrix / Synapse runtime data — live config (with secrets), signing keys and
# logs are generated on the VM. Only the committed *.example template stays in
# git (mirrors .env.example -> .env).
/synapse/*
!/synapse/homeserver.yaml.example

# IDE & OS
.idea/
.vscode/
*.swp
.DS_Store

# Claude Code (local-only settings)
.claude/settings.local.json
